#!/bin/bash
# .githooks/pre-push

echo "🛡️  Running Pre-Push Check..."

# Ensure the Java source compiles before pushing
javac SalesRoundingDecimal.java

if [ $? -ne 0 ]; then
 echo "❌ Build failed! Fix errors before pushing."
 exit 1
fi

echo "✅ Build passed. Pushing now..."
exit 0